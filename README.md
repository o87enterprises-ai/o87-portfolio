# Hugging Face Spaces Configuration
# This file tells HF Spaces how to run your app

# Since this is a static HTML site, we'll use the static backend
sdk: static

# Optional: Specify Python version if needed
# python_version: "3.11"

# Optional: System packages if needed
# system_packages:
#   - nginx
